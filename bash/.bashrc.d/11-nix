# shellcheck disable=SC2148
# vi:syntax=sh
# Nix Goodness

# Will only exist on non-NixOS machines
if [ -e '/etc/profile.d/nix.sh' ]; then
  # shellcheck source=/dev/null
  . '/etc/profile.d/nix.sh'
fi
